import { ReactNode } from "react"

interface FindingProps {
  label?: string
  children?: ReactNode
}

// TODO: implement
export const Finding = ({ label, children }: FindingProps) => {
  return null
}
